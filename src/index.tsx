/* @refresh reload */
import { render } from "solid-js/web"

import SlideLaForMarkingTime from "./App"
import "./index.css"

const root = document.getElementById("root")

render(() => <SlideLaForMarkingTime />, root!)
