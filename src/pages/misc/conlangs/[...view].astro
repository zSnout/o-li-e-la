---
import { ALL_VIEWS } from "../../.."
import Layout from "../../../Layout.astro"
import { Main } from "./_index"

export function getStaticPaths() {
  return ALL_VIEWS.map((view) => ({ params: { view: view } }))
}
---

<Layout>
  <Main view={Astro.params.view} client:load />
</Layout>
